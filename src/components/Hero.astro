---
// Hero component - Full viewport with industrial cocktail bar atmosphere
---

<section id="hero" class="relative min-h-screen flex items-center justify-center overflow-hidden">
  <!-- Background Gradient (placeholder for actual image) -->
  <div class="absolute inset-0 bg-gradient-to-br from-[#1a1a1a] via-[#2a1f1a] to-[#1a1a1a]">
    <!-- Atmospheric overlay patterns -->
    <div class="absolute inset-0 opacity-30 bg-[radial-gradient(ellipse_at_top_left,_var(--tw-gradient-stops))] from-[#b87333]/20 via-transparent to-transparent"></div>
    <div class="absolute inset-0 opacity-20 bg-[radial-gradient(ellipse_at_bottom_right,_var(--tw-gradient-stops))] from-[#8b4513]/30 via-transparent to-transparent"></div>

    <!-- Subtle texture overlay -->
    <div class="absolute inset-0 opacity-5 bg-[url('data:image/svg+xml,%3Csvg viewBox=%220 0 200 200%22 xmlns=%22http://www.w3.org/2000/svg%22%3E%3Cfilter id=%22noise%22%3E%3CfeTurbulence type=%22fractalNoise%22 baseFrequency=%220.65%22 numOctaves=%223%22 stitchTiles=%22stitch%22/%3E%3C/filter%3E%3Crect width=%22100%25%22 height=%22100%25%22 filter=%22url(%23noise)%22/%3E%3C/svg%3E')]"></div>
  </div>

  <!-- Dark overlay for text readability -->
  <div class="absolute inset-0 bg-gradient-to-t from-[#1a1a1a] via-[#1a1a1a]/50 to-[#1a1a1a]/70"></div>

  <!-- Content -->
  <div class="relative z-10 text-center px-4 sm:px-6 lg:px-8 max-w-4xl mx-auto">
    <!-- Main Title -->
    <h1 class="mb-4">
      <span class="block font-serif text-7xl sm:text-8xl md:text-9xl font-bold text-[#f5f5dc] tracking-wider mb-2 drop-shadow-2xl">
        LOFT
      </span>
      <span class="block font-serif text-2xl sm:text-3xl md:text-4xl text-[#b87333] tracking-[0.3em] uppercase">
        Bar Kelham
      </span>
    </h1>

    <!-- Decorative Line -->
    <div class="flex items-center justify-center my-8">
      <div class="w-16 h-px bg-gradient-to-r from-transparent via-[#b87333] to-transparent"></div>
      <div class="w-2 h-2 mx-4 rotate-45 border border-[#b87333]"></div>
      <div class="w-16 h-px bg-gradient-to-r from-transparent via-[#b87333] to-transparent"></div>
    </div>

    <!-- Tagline -->
    <p class="text-lg sm:text-xl md:text-2xl text-[#f5f5dc]/80 font-light tracking-wide max-w-2xl mx-auto mb-12">
      Hidden in Kelham Island
    </p>

    <!-- CTA Button -->
    <a
      href="#about"
      class="inline-block px-8 py-4 border-2 border-[#b87333] text-[#b87333] font-medium uppercase tracking-widest text-sm
             hover:bg-[#b87333] hover:text-[#1a1a1a] transition-all duration-300
             focus:outline-none focus:ring-2 focus:ring-[#b87333] focus:ring-offset-2 focus:ring-offset-[#1a1a1a]"
    >
      Discover The Loft
    </a>
  </div>

  <!-- Scroll Indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10">
    <a href="#about" class="flex flex-col items-center text-[#f5f5dc]/60 hover:text-[#b87333] transition-colors duration-300 group">
      <span class="text-xs uppercase tracking-widest mb-3 opacity-0 group-hover:opacity-100 transition-opacity duration-300">Scroll</span>
      <div class="w-6 h-10 border-2 border-current rounded-full flex justify-center pt-2">
        <div class="w-1 h-2 bg-current rounded-full animate-bounce-slow"></div>
      </div>
    </a>
  </div>
</section>

<style>
  @keyframes bounce-slow {
    0%, 100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(6px);
      opacity: 0.5;
    }
  }

  .animate-bounce-slow {
    animation: bounce-slow 2s ease-in-out infinite;
  }
</style>
